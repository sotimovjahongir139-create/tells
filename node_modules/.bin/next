#!/usr/bin/env node
// Shim: pm2 saved config may point here from old Next.js setup.
// Route everything to the Express server regardless of args.
require(require('path').resolve(__dirname, '..', '..', 'server.js'));
